#include <iostream>
#include <vector>
#include <string>

/*int main() {
	while (true)
	{
		std::string str, str2;
		std::cout << "Element : " << std::endl;

		std::cin >> str;
		getline(std::cin, str2);
		int a = -1;
		int b = -1;
		std::size_t space = str2.find(" ");
		str2 = str2.substr(space + 1);

		if (str != "add" && str != "delete" && str != "info" && str != "sort" && str != "clean") {
			a = std::stoi(str);
		}
		if (str2 != "basics") {
			b = std::stoi(str2);
		}
		std::cout << "a:" << " " << a << std::endl << "b:" << " " << b << std::endl << "str:" << " " << str << std::endl << "str2:" << " " << str2 << std::endl;
	}

	return 0;
*/
